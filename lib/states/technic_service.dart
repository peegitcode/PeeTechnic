import 'package:flutter/material.dart';

class TechnicService extends StatelessWidget {
  const TechnicService({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}